---
interface Props {
  columns?: 1 | 2 | 3 | 4;
  class?: string;
}

const { columns = 1, class: className } = Astro.props;
---

<div class:list={['grid', columns > 1 && `grid--${columns}`, className]}><slot /></div>
