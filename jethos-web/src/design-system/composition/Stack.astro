---
interface Props {
  gap?: 'sm' | 'md' | 'lg';
  class?: string;
}

const { gap = 'md', class: className } = Astro.props;
---

<div class:list={['stack', `stack--${gap}`, className]}><slot /></div>
