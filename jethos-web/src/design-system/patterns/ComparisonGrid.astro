---
interface Props {
  label: string;
  class?: string;
}

const { label, class: className } = Astro.props;
---

<div class:list={['comparison-grid', className]} role="table" aria-label={label}><slot /></div>
