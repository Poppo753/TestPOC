---
interface Props {
  label: string;
  class?: string;
}

const { label, class: className } = Astro.props;
---

<div
  class:list={['table-wrap', className]}
  role="region"
  aria-label={label}
  tabindex="0"
  data-scrollable-region
>
  <slot />
</div>
